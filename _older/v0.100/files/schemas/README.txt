This directory contains schemas for the "obs-info" files and a Java code that
can be used to validate files against the schemas.  
The script validate.sh runs the Java validator.  
Alternatively, you can use online YAML-> JSON converters and JSON 
schema validators.