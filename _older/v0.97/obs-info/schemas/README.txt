This directory contains schemas for the "obs-info" files and a JAVA code that
can be used to validate each type of file.  The script validate.sh runs the
validator.  Alternatively, you can use online YAML-> JSON converters and JSON 
schema validators.